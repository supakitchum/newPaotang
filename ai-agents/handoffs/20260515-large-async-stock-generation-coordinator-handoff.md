# Large Async Stock Generation Coordinator Handoff

## Agent

Coordinator

## Task

Open implementation work for large asynchronous stock generation.

## Source Request

User asked to implement the accepted plan:

```text
generate total ให้มากกว่า 10000
อยากได้หลักแสนหลักล้าน
ทำเป็น job เพื่อจัดการปริมาณข้อมูล
เลข 6 หลักซ้ำได้
ห้ามใช้ insertOrIgnore เพราะเลข 6 หลักซ้ำได้
```

## Coordinator Decision

Decision file:

```text
ai-agents/decisions/20260515-large-async-stock-generation-decision.md
```

Coordinator is opening this through Orchestrator because the work touches:

```text
apps/platform-api/**
apps/back-office/**
docs/openapi.yaml
docs/coordinator-agent-handoff.md
ai-agents/tasks/**
ai-agents/reports/**
```

## Critical Requirement

Do not use `insertOrIgnore` for stock row generation.

Reason:

```text
full_number values may repeat
duplicate 6-digit lottery numbers are valid product behavior
dedupe must not be based on full_number
stock id uniqueness must come from deterministic stock id generated from batch_id + global_index + number
```

Retry safety must come from chunk status/transaction design, not from ignoring duplicate insert rows.

## Scope For Orchestrator

Create task prompts for:

```text
Backend Develop
BO Develop
QA Tester
```

Recommended order:

```text
Backend Develop -> BO Develop -> QA Tester -> Coordinator
```

Backend owns:

```text
large async generation backend
migrations for batch progress and chunk tracking
GenerateStockBatchChunkJob
batch list/detail APIs
OpenAPI updates
backend tests
image dispatch separation after stock completion
queue config/docs
```

BO owns:

```text
remove 10,000 UI cap
large total support
progress polling from batch endpoints
failure display
preserve linked quota inputs
preserve current game default and no ALL option
```

QA owns:

```text
Docker-only backend and BO validation
real async generation workflow using test DB
12,000 row completion proof
duplicate full_number proof
idempotency replay/conflict proof
progress polling evidence
runtime restore/login smoke
```

## Important Rules

- Orchestrator must not implement code.
- Backend/BO must receive task briefs before implementation.
- Use Docker only for app commands.
- QA destructive commands must use `APP_ENV=testing`, `DB_DATABASE=newpaotang_test`, and `--env=testing`.
- QA must not wipe runtime DB `newpaotang`.
- Runtime smoke after QA must use non-destructive seed/smoke only.

## Acceptance Summary

```text
POST /admin/central/stock/generate supports totals above 10,000
large generation returns queued/processing batch immediately
chunk jobs generate rows in progress-tracked chunks
normal bulk insert is used, not insertOrIgnore
duplicate full_number values remain present when generated by multiple rounds
batch list/detail endpoints expose progress
BO polls progress and removes the old 10,000 cap
image generation starts after stock batch completion through separate dispatcher
QA passes with isolated test DB evidence
```

## Next Agent

Orchestrator

