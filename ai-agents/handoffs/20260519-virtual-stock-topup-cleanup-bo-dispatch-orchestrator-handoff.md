# 20260519 Virtual Stock Topup Cleanup - BO Dispatch Orchestrator Handoff

Task: `virtual-stock-topup-cleanup-bo-dispatch`
Agent: Orchestrator

## Context

Backend Develop completed and pushed the backend dependency for:

```text
virtual-stock-topup-cleanup-backend
```

Backend handoff:

```text
ai-agents/handoffs/20260519-virtual-stock-topup-cleanup-backend-handoff.md
```

Backend implementation commit:

```text
c61b8ef5bd6612d1734616acb8a6f1d259e4d5c4
```

Backend handoff commit on shared history:

```text
d03ce97
```

## Backend Contract BO Must Use

Primary BO task:

```text
ai-agents/tasks/20260519-virtual-stock-topup-cleanup-bo.md
```

Key backend behavior:

```text
POST /admin/central/stock/generate is virtual generate/top-up only
first generate creates active profile/container and first supply layer
later generate requests for the same tenant/game add active supply layers
seed is server-managed and not required from BO/API callers
idempotency replay returns the original batch/profile/layer without double-adding supply
changed payload under the same idempotency key returns idempotency conflict
legacy physical/quota fields and non-virtual modes are rejected by validation
```

Response metadata BO may display where useful:

```text
stock_mode
profile_id
layer_id
layer_capacity
total_capacity
top_up
```

Stock Generation detail contract:

```text
combined virtual capacity across active layers
virtual_copies
owner.type
owner.label
unassigned/no_agent for virtual-only copies without partner/agent/materialized owner
real image fields only from materialized stock_items/local_stock_items
no fabricated image rows for virtual-only copies
```

Stock Pattern Coverage realtime contract:

```text
channel: private-admin.central.stock.coverage.game.{game_id}
event: stock.coverage.updated
```

Backend emits coverage updates for:

```text
generation/top-up
limit setting changes
per-pattern override changes
reservation/release
sold conversion counter updates
```

BO must keep HTTP reload as source-of-truth fallback for reconnects, missed events, or incomplete deltas.

## Orchestrator Review

I reviewed the Backend handoff and found enough contract detail for BO to proceed:

```text
additive supply-layer model documented
generate/top-up contract documented
internal seed and idempotency behavior documented
detail ownership/image behavior documented
coverage websocket channel/event and emit points documented
legacy physical/quota retirement documented
Docker validation and test DB isolation reported
```

No app implementation files were edited by Orchestrator.

## Dirty Worktree Note

At dispatch time, the only local dirty file observed in this worktree was:

```text
apps/platform-api/.phpunit.result.cache
```

It was generated by backend tests, not staged by Orchestrator, and should remain unrelated to BO work.

## Next Agent

BO Develop
