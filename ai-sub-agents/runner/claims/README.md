# Runner Claims

Runner claim files prevent multiple runners from starting the same trigger.

Naming:

```text
YYYYMMDD-<task-key>-<agent>-trigger.claim.md
```

Claims must be created atomically with no overwrite.
