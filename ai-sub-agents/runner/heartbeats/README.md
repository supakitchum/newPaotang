# Runner Heartbeats

Heartbeat files show that a RUNNING trigger is still alive.

Naming:

```text
YYYYMMDD-<task-key>-<agent>-trigger.heartbeat.md
```

Default timeout:

```text
timeout_minutes: 30
max_retries: 1
```
