apiVersion: apps/v1
kind: Deployment
metadata:
  name: customer
  labels:
    app.kubernetes.io/name: customer
    app.kubernetes.io/part-of: newpaotang
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: customer
  template:
    metadata:
      labels:
        app.kubernetes.io/name: customer
        app.kubernetes.io/part-of: newpaotang
    spec:
      terminationGracePeriodSeconds: 60
      containers:
        - name: customer
          image: newpaotang/customer:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 3000
          envFrom:
            - configMapRef:
                name: newpaotang-customer-config
          readinessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 10
            periodSeconds: 15
          livenessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 30
            periodSeconds: 30
          resources:
            requests:
              cpu: 150m
              memory: 256Mi
            limits:
              cpu: 750m
              memory: 768Mi
---
apiVersion: v1
kind: Service
metadata:
  name: customer
  labels:
    app.kubernetes.io/name: customer
spec:
  selector:
    app.kubernetes.io/name: customer
  ports:
    - name: http
      port: 3000
      targetPort: http
